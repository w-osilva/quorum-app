<h1>Legislators</h1>

<table>
  <tr>
    <th>id</th>
    <th>Name</th>
    <th>Actions</th>
  </tr>

  <% @legislators.each do |legislator| %>
    <tr>
      <td><%= legislator.id %></td>
      <td><%= legislator.name %></td>
      <td><%= link_to 'Show', legislator %></td>
    </tr>
  <% end %>
</table>